Open the script in a text editor for detailed setup and configuration options.

This is a cgi program to grab all the fields from a web page and e-mail them to the location specified by an hidden HTML tag and the relocate the user to a location specified in a hidden HTML tag